{
  "name": "price-intel-api",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time e-commerce price intelligence API for AI agents — electronics pricing, price history, and deal detection across Amazon. Pay-per-query via x402 (USDC on Base).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.99.0"
  }
}
